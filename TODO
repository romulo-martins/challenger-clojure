# Todo do desafio
[x] Representação dos dados do cliente (nome, cpf, email);
[x] Representação dos dados do cartão (número, cvv, validade, limite);
[x] Listagem de compras realizadas (data, valor, estabelecimento, categoria);
[x] Valor dos gastos agrupados por categoria;
[x] Cálculo do valor da fatura do mês (opcional);
[x] Busca de compras pelo valor ou estabelecimento (opcional).

# Todo pessoal
[ ] Incluir os dados do usuário e cartão de credito na fatura.
[ ] Refatorar logicamente as funções em arquivos separados. 

[ ] Refatorar o cálculo do valor da fatura do mês (opcional).
    - Em uma nova branch, substituir a implementação manual do helper de data para utilizar alguma lib para tratar datas.
    - Sugestão: https://github.com/clj-time/clj-time 